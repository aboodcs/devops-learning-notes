apiVersion: v1
kind: Service
metadata:
  labels:
    app: web
  name: web
  namespace: easy-exam-01
spec:
  ports:
  - port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: web
  type: NodePort
status:
  loadBalancer: {}
